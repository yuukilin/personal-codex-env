# Automations Templates

這裡只保存 Codex automations 的 `automation.toml` 模板。

注意：

- 不要把 `last-run.md`、`memory.md`、logs 或執行輸出放進 repo。
- 兩台 Mac 不要同時啟用同一個 automation。
- 新 Mac 套用前先檢查 prompt 裡的本機路徑是否存在。
- 若要從 M2 切到 M5，先在 M2 暫停 automation，再在 M5 建立或啟用。
